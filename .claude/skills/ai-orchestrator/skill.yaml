name: ai-orchestrator
description: |
  多AI协作编排技能 - 提供3种编排模式，按需选择

  【3种编排命令】
  1. /ai-team   = Claude + Codex + Gemini（完整三引擎：分析→生成→审查）
  2. /ai-code   = Claude + Codex（快速开发：分析→生成，无审查）
  3. /ai-review = Claude + Gemini（代码审查：分析→审查，无生成）

  【选择指南】
  - 生产级代码 → /ai-team（完整流程，质量最高）
  - 快速原型 → /ai-code（跳过审查，速度最快）
  - 审查现有代码 → /ai-review（专注审查，最细致）

  【输出目录】
  所有中间结果保存在 .ai-orchestrator/ 目录，可追溯、可回滚

  【完成后规范】
  每次完成任务后，必须在 README.md 的「更新日志」章节添加更新记录：
  - 格式：`### YYYY-MM-DD` + 更新内容摘要
  - 内容：简述本次完成的功能、修复的问题、重构的模块等
  - 位置：README.md 底部的「📝 更新日志」章节，最新的放最上面

version: "1.0.0"

author: 老金

# Skill command configuration
commands:
  - name: orchestrate
    description: 启动AI多引擎编排流程
    script: scripts/orchestrate.sh
    args:
      - name: task
        description: 要完成的任务描述
        required: true
        type: string

# Dependencies
dependencies:
  required:
    - claude  # Claude Code (installed)
  optional:
    - codex   # Codex CLI (code generation)
    - gemini  # Gemini CLI (code review)

# Configuration options
config:
  output_dir: ".ai-orchestrator"
  auto_cleanup: false
  verbose_logging: true
  max_retries: 1

# Usage examples
examples:
  - name: 简单任务
    command: orchestrate "实现用户登录功能"

  - name: 复杂任务
    command: orchestrate "实现JWT登录功能，包含注册、登录、token刷新"

  - name: 系统设计
    command: orchestrate "设计RBAC权限系统，包含角色管理、权限分配、访问控制"
